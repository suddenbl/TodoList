import { create } from 'zustand';

// interface todoStoreInterface {
//     todos: Object[]
// }

// export const useTodosStore = create((set) => ({
//   todos: [],
//   setTodos: () => set((todos) => ({ todos })),
// }));
